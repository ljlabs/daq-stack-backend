{
  "name": "cloud_gate_way",
  "version": "1.0.0",
  "description": "a webserver to accept and save images from the client app",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/connect-busboy": "0.0.2",
    "@types/cors": "^2.8.6",
    "@types/express": "^4.16.1",
    "@types/fs-extra": "^8.0.0",
    "@types/node": "^12.0.4",
    "tslint": "^5.17.0",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "@types/moment": "^2.13.0",
    "axios": "^0.19.0",
    "connect-busboy": "0.0.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "fs-extra": "^8.1.0",
    "moment": "^2.24.0"
  }
}
